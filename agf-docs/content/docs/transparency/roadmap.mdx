---
title: "Roadmap"
description: "What AGF is working on next — open items, planned work, and what is intentionally not yet scoped."
---

AGF is maintained by a single author in public, with explicit confidence levels and acknowledged open questions. The roadmap below reflects the current state of work after v0.2.0; it is updated when meaningful items ship or land in the queue.

This page is a living summary. The detailed work queue lives in `BACKLOG.md` (private). Major decisions surface in [Decisions](/docs/transparency/decisions). Released work surfaces in the [Changelog](/docs/transparency/changelog).

## Now (in flight)

<Confidence level="informed">

- **Cohesion follow-through.** Slogan-density reduction across flagship MDX (G5-F18); transparency pages (this set, shipping with this update). Targets the residual coherence findings from external review GPT-5.4 (2026-04-23).
- **AI Engineering profile rewrite.** Retire the "Phase 1–5" H3 headings in favor of the four canonical Composition Patterns; regenerate the implementation-progression diagram. Pairs with the AI Engineer audience served by the new [First 30 Days](/docs/overview/first-30-days) and [Reference Walkthrough](/docs/overview/reference-walkthrough) pages.

</Confidence>

## Next (queued, not yet started)

<Confidence level="informed">

- **GDR cross-doc integration.** Update `grc-profile` Evidence Artifacts table to reference GDR for Primitive #8 (decision records) and Primitive #11 (promotion/demotion records). Add `decision_id` references to observability event payloads in `observability-profile`.
- **Composability Interface dual-form audit.** Per D5 / D16, audit each Ring Control Signal and Gate Resolution for explicit dual-form annotations. Pair with Primitive #11 (Trust Ladders) audit — promotion/demotion paths are Domain Outcomes that must emit GDRs.
- **Agentic Compliance Blind Spots analysis.** Public 10-gap analysis surfacing where current control catalogs (AICM, NIST 800-53, ISO 42001) leave agent-specific failure modes uncovered. The "gap disclosure" commitment from the [Relationship to Frameworks](/docs/reference/relationship-to-frameworks) doc made operational.

</Confidence>

## Soon (planned but not yet scoped)

<Confidence level="open">

- **Primitive #14 Identity & Attribution upgrade.** Ten-point spec already drafted; integrates Microsoft and CSA agentic-identity research. Scope: distinguish agent identity from delegated user identity; add credential lifecycle.
- **Primitive #15 → "Agent SRE."** Rename and reframe to absorb SLO definition, error-budget policy, and chaos engineering for agentic systems.
- **TOGAF / SABSA / COBIT crosswalks.** Currently described as "what sits above the AGF stack" without explicit mapping. Crosswalks deferred until adopter demand surfaces.
- **First adopter case studies.** AGF currently has zero published implementations. The "typically implement both" claims in [Trust Ladders](/docs/overview/trust-ladders) and [Relationship to Frameworks](/docs/reference/relationship-to-frameworks) are flagged as `<Confidence level="open" />` until adopter data exists.

</Confidence>

## Not on the roadmap (intentional)

<Confidence level="established">

- **AGF is not a runtime.** No reference implementation is planned. Install Microsoft AGT, AWS-native agent governance, or whatever runtime your organization converges on.
- **AGF is not a certification.** CSA STAR for AI and ISO 42001 are the relevant assurance programs.
- **AGF is not a control catalog.** AICM, NIST 800-53, and ISO 42001 are the catalogs.
- **AGF is not consensus.** AGF is synthesized by a single author; the BDFL governance model is intentional. See [`GOVERNANCE.md`](https://github.com/jessepike/agf/blob/main/GOVERNANCE.md).

</Confidence>

## How priorities are set

Items move from **Soon** → **Next** → **Now** when they pass three gates:

1. **Coherence** — does the change reconcile to existing canonical primitives, or does it require a new Decision entry?
2. **Confidence** — can the change ship with a defensible confidence label (`established` / `informed` / `open`)?
3. **Concreteness** — is the deliverable a specific artifact (doc, schema, diagram, primitive) rather than a research direction?

If you want to influence the roadmap, the highest-leverage paths are: contribute evidence from a real implementation, challenge a `<Confidence level="informed" />` claim with counter-evidence, or open a discussion at [GitHub Discussions](https://github.com/jessepike/agf/discussions).
