# Decision Log

Captures terminology, structural, and positioning decisions with rationale. Intended to survive across sessions so future contributors understand *why* choices were made.

| # | Date | Decision | Rationale | Alternatives Considered |
|---|------|----------|-----------|------------------------|
| 1 | 2026-03-19 | Replace "reliable" with "durable" in the five system properties (safe, secure, durable, auditable, observable) | "Reliable" is generic — every framework claims it. "Durable" adds a distinct architectural property: systems that survive failures, maintain state across interruptions, and preserve governance guarantees over time. The term resonates with the practitioner audience who understands durable execution. | Keep "reliable" (defensible but weak); drop to four properties (tighter but loses a real concern); "accountable" (human property, not a system property — mismatch in a list describing system characteristics) |
| 2 | 2026-03-19 | Replace "prior art" with "existing work" throughout core docs | "Prior art" is patent/legal jargon that creates unnecessary friction for non-academic readers. "Existing work" is immediately clear to any audience — security practitioners, compliance teams, executives, engineers. | "Foundations" (good but implies hierarchy); "related work" (academic convention, same jargon problem); "building blocks" (too informal for framework tone) |
| 3 | 2026-04-21 | **AGF Positioning Pillars.** Six pillars define what AGF is: (1) Category stake = Agentic Governance (narrower than AI governance); (2) Four verbs — Synthesizes / Unifies / Prescribes / Operationalizes (causal order); (3) OTAA invariant — every AGF primitive MUST be Observable, Traceable, Auditable, Agent-operable; (4) Dual-form principle — "machine-consumable, human-decidable" (every primitive in human and machine form simultaneously); (5) Tempo taxonomy — wire-speed enforcement (<1ms), near-realtime supervision (sec–min), human-speed governance (hours–weeks); (6) NIST CSF parallel — AGF Rings are to agentic governance what CSF Functions are to cybersecurity. | Supersedes earlier "integration layer" framing. "Integration layer" remains accurate as a description of what AGF *does for a reader*, but "synthesis + unification + prescription + operationalization" is what AGF *is*. Different audiences, not in conflict. | "AI governance framework" (too broad, blends with every competitor); "framework of frameworks" (accurate-but-weak, no commitments); "responsible AI framework" (wrong category — RAI is upstream of agentic). |
| 4 | 2026-04-21 | **Seven-layer stack: where control catalogs and risk quantification fit.** AGF sits at Layer 0 as architectural substrate. Layer 1 = OWASP Agentic Top 10 (threat baseline). Layer 2 = CSA MAESTRO + Microsoft Failure Mode Taxonomy (threat modeling). Layer 3 = control catalogs — CSA AICM, ISO 42001/27001, NIST 800-53, EU AI Act Annexes, BSI AIC4. Layer 4 = CSA Agentic Trust Framework (operating model). Layer 5 = Microsoft AGT + CAF + Agent 365 (runtime reference). Layer 6 (orthogonal) = risk quantification — FAIR, FAIR-CAM, ISO 31000, NIST AI RMF Measure. AGF primitives crosswalk to Layer 3 catalogs; AGF does not replicate catalog content. FAIR and related methodologies are methodology, not primitive — they integrate via AGF's Decision Intelligence capability. | The landscape is crowded. Without an explicit stack, any organization asks "do I need AGF or do I need X?" With the stack, the question resolves: you need AGF *and* X, because they live at different layers. Layer 0 names AGF's position unambiguously. | Absorb FAIR as a primitive (conflates methodology with pattern); position as a control catalog (AGF is not a catalog — see entry 6); flat "framework ecosystem" diagram (no architectural story, no composition logic). |
| 5 | 2026-04-21 (refined 2026-04-22) | **Dual-form is load-bearing at gate boundaries.** Refinement of the dual-form principle. Every Gate Resolution and every Domain Outcome MUST emit both a human-readable rationale AND a machine-readable Governance Decision Record (GDR) simultaneously — same decision, two audiences, same moment. "Gate boundary" is defined per #8 (Gate Vocabulary Disambiguation): Gate Resolutions and Domain Outcomes. The GDR (#9) is the canonical machine-form artifact. The original wording referenced the Ring Control Signal enum (`PASS / REVISE / HALT / GATE / ERROR`) as "gate decisions" — #8 disambiguates the four-part vocabulary; the principle is unchanged. | Dual-form is a general principle for all primitives; at gate boundaries it becomes non-negotiable. A gate that emits only human-form forces machine systems to do NLP; a gate that emits only machine-form leaves auditors with raw JSON. Both forms required, always, at gates. Affects Primitive #8 (Governance Gates), the Composability Interface spec, and Primitive #11 (Trust Ladders, whose promotions/demotions are Domain Outcomes that emit GDRs). | "Machine form at gates only when needed" (weakens the invariant — once optional, rapidly skipped); "human form as a derived artifact" (breaks OTAA agent-operability if machine form isn't the primary emission); no gate-boundary invariant at all (leaves dual-form as aspirational). |
| 8 | 2026-04-22 | **Gate Vocabulary Disambiguation: four-part vocabulary.** AGF's gate-related vocabularies are NOT alternative spellings of one concept. They form a four-part vocabulary of distinct, scoped enums. **Ring Control Signal** (Composability Interface, `agentic-primitives.md` Composability Interface section): `PASS / REVISE / HALT / GATE / DELEGATE / ERROR` — how primitives in adjacent rings signal each other. **Gate Resolution** (Primitive #8 Governance Gates; also the return value of the `GATE` Ring Control Signal): `APPROVE / REJECT / MODIFY / DEFER / ESCALATE` — the authorizer's response when a gate fires. **Domain Outcome** (domain applications such as Tool Gate): domain-defined enums (e.g., `Authorized / Conditionally Authorized / Denied`) that map onto Gate Resolutions. **Audit Artifact**: GDR (Governance Decision Record) — see entry #9 — the canonical serialized record format for Gate Resolutions and Domain Outcomes. Refines the imprecise "gate decision (PASS/.../ERROR)" wording in earlier prose (#5, internal D12.4a, `relationship-to-frameworks.md`), which conflated Ring Control Signals with Gate Resolutions. The model is unchanged — `agentic-primitives.md` already nests these vocabularies correctly: GATE is a Ring Control Signal that returns a Gate Resolution. | The four vocabularies were each correct in their home doc but were referenced loosely in cross-doc prose, creating apparent ambiguity for readers and blocking schema work for the dual-form gate audit. Naming the four parts explicitly: (a) preserves the existing model (no primitive changes), (b) eliminates the apparent conflict, (c) clarifies what "gate boundary" means for dual-form (#5), (d) makes the GDR primitive's role unambiguous as the canonical audit artifact. Deliberate choice to use names not ordinal numbers in prose to avoid collision with the seven-layer stack (#4) and maturity L1–L5 (#6). | Collapse to one enum (rejected — surfaces serve genuinely different concerns); rename one of the existing enums (rejected — both well-established and accurately scoped); document in shared-vocabulary without a decision entry (rejected — wording bug propagated across DECISIONS.md, internal log, and a public framework doc; needs decision-level visibility); use ordinal labels L1–L4 (rejected — collides with seven-layer stack and maturity levels). |
| 9 | 2026-04-22 | **Governance Decision Record (GDR) is the canonical audit artifact at gate boundaries.** Promotes Q1 from the change queue. The GDR is a reusable AGF primitive — a single record schema with polymorphic content — that serializes any Gate Resolution or Domain Outcome into an auditable artifact. **Scope:** GDR emission required at every Gate Resolution (Primitive #8 fire) and every Domain Outcome (e.g., Tool Gate authorization, Trust Ladder promotion/demotion). Ring Control Signals continue to emit observability events per Primitive #10 (`gate_triggered`, `gate_resolved`, etc.); GDR is the *record* of the decision, observability events are the *log* that the decision occurred. Both are emitted at gate resolution; observability events reference the GDR by `decision_id`. **Schema:** required sections are `metadata`, `decision`, `subject`, `inputs`, `rationale`, `conditions`, `authorization`, `audit`. Canonical schema published as `docs/schemas/gdr.yaml` (structural validation only; lifecycle invariants are normative in the GDR doc prose). **Lifecycle:** GDRs carry `status: pending \| resolved \| expired \| superseded \| aborted`. DEFER is a resolved decision (authorizer actively deferred); a successor GDR with `supersedes` ref captures the eventual real resolution. HALT-on-timeout transitions a pending GDR to `aborted` with the ring-layer HALT signal emitted as observability event. **Sensitive content:** GDRs MUST store classifications, hashes, and access-controlled URIs for sensitive inputs — never raw payloads. **Standards:** GDR provides evidence in support of regulatory record-keeping requirements (EU AI Act Art. 12, NIST AI RMF MANAGE 4.1 logging requirements, ISO 42001 documentation clauses); does not by itself satisfy them. Distinct from an Architectural Decision Record (ADR), which captures engineering design choices. First domain application: Tool Gate (Q3, separate promotion). | Two pressures land on the same artifact: (a) the dual-form gate-boundary requirement (#5) needs a canonical schema target, and (b) the existing grc-profile evidence table already promises "human decision records" for Primitive #8 and "promotion/demotion records" for Primitive #11 without specifying a format. Promoting GDR resolves both with one artifact and makes the grc-profile evidence promises concrete. Naming GDR as a primitive (not "an artifact emitted by Tool Gate") is what makes the format reusable across domains. Scoping to gate boundaries (not every ring signal) preserves observability event volume and matches original D5 intent. | Define separately per primitive (rejected — would create N artifact formats with no cross-primitive audit consistency); leave GDR inside the Tool Gate concept doc (rejected — couples a domain-application doc to the framework's audit primitive); use AICM control evidence schemas as the format (rejected — different artifact class entirely); name it "Governance Decision Artifact" or "Decision Record" (considered — "Record" matches industry usage [ADR, DDR]; "Governance" disambiguates from ADR); broader scope to all Ring Control Signals (rejected per external review — would be a new normative requirement unrelated to D5's original gate-boundary intent and would expand observability storage cost significantly). |
| 6 | 2026-04-21 | **Maturity Model — rename L1 to "Non-existent"; absorb inventory/classification content into L2 Foundation.** AGF retains its 5-level program maturity model. L1 is renamed from "Awareness" to "Non-existent" to honestly name the state most organizations are in today. The old L1 content (agent inventory exists, basic risk classification) is absorbed as L2 Foundation entry criteria. L2–L5 labels unchanged. AGF's L1–L5 is a program-level scale complementary to — not substituted for — CSA ATF's 4-tier per-deployment autonomy scale (Intern → Junior → Senior → Principal). | "Awareness" implies prior investment (inventory, classification) that most organizations with agents in production have not made. "Non-existent" is the honest floor. Makes L1 → L2 Foundation the headline governance-investment story and gives AGF a clear ask. | Keep "Awareness" (dishonest about the starting state); adopt full CMMI 0–5 scale (discards AGF's specific L2–L5 semantics that the community has started adopting); add a new L0 (breaks the 5-level structure). |
| 10 | 2026-04-24 | **Cohesion Pass v0.3 — Vocabulary Reconciliation (three-part).** Three vocabulary alignments resolving drift surfaced by 2026-04-23 external review (GPT-5.4). **(a) Composition Patterns canonicalized at four:** Minimum Viable Control, Validation Pipeline, Governed Decision Flow, Full Governed Agentic System. The prior "Phase 1–5" language on the Rings Model page is retired; Ring activation is a consequence of pattern selection, not a parallel axis. The #11/#15/#18 hardening stack that earlier drafts named as a fifth "Secure Governed System" pattern is demoted to a **hardening posture** — a modifier applied within GDF (or as a precondition for entering Full Governed), expressed as posture language ("a production-hardened Governed Decision Flow applying #11/#15/#18"), never as a named step in the progression. Naming it as a peer pattern was redundant (it is "Full Governed minus Ring 3 learning") and worsened naming density (G5-M02). **(b) "Governance" qualifier conventions:** the bare noun is overloaded across five senses (the framework AGF itself, Ring 2, AGF program maturity, Primitive #8 Gates, NIST CSF Govern function). Qualified forms adopted — "AGF" for the framework (never "governance" alone), "the Governance ring" or "Ring 2", "AGF program maturity" (retires "program-level governance"), "Governance Gates" capitalized, "NIST CSF Govern" always prefixed. Bare "governance" reserved for sense #1 or deliberate adjectival use ("governance posture"). **(c) Observability three-layer disambiguation:** Primitive #10 (Event-Driven Observability) emits; Agentic Observability (concept doc) correlates; Observability Profile is the role-based implementation guide. Hierarchy now stated at top of the concept doc (canonical + site) and in a dedicated `shared-vocabulary.md` section. Closes findings G5-F09, G5-F15, G5-F16 (Cohesion Pass v0.3 per `docs/findings-ledger.md`). Meta-finding G5-M02 (naming density) requires holistic re-read — not closed by this change alone. | External review surfaced three coherence findings: the composition-patterns page said 4, the rings-model page said 5 (using "phase" language), the vocabulary listed 5 with different labels — three surfaces, three stories; "governance" was used with 5+ senses in the same sentence on public pages; observability appeared as Primitive #10, as a capability concept doc, and as a Profile with no stated hierarchy. Left unresolved, Cluster D (Actionability) content would inherit and re-encode these conflations. Cohesion Pass runs before new-content work to provide a clean-vocabulary substrate. The choice of 4 patterns (rather than 5) dogfoods G5-M02: capitalizing a construct whose content is "Full Governed minus Ring 3" creates a named peer for what is structurally a modifier, worsening naming density to mask a gap the vocabulary already covered. | (a) 5-pattern canon with Secure Governed System rejected (creates a named construct for what is structurally a hardening posture modifier; worsens G5-M02; redundant with "Full Governed minus Ring 3"). Orthogonal 5×5 patterns × phases matrix rejected (worsens G5-M02 further). (b) Renaming "governance" across the site rejected (too invasive across hundreds of surfaces; qualifier discipline is lower cost and preserves the existing term). (c) Renaming "Agentic Observability" → "Unified Observability" rejected (slug was just renamed 2026-04-24 per G5-F19; second rename in the same week is churn exceeding the clarity gain). |
| 7 | 2026-04-21 | **Harness = governance envelope around an AI agent (source + runtime adapters, inseparable).** Formalizes "harness" as an AGF term with the industry-standard meaning. A harness is the declarative source artifacts (intent, constraints, capabilities, policy, evaluation criteria) PLUS the SDK-specific runtime enforcement adapters that translate them into deterministic controls at call time. Formula (industry convention): `Agent = Model + Harness`. Tiered model per AGF Agent Harness Spec v0.3 — Tier 0 (Provider, precondition) / Tier 1 (Agent, maps to AGF Security Fabric) / Tier 2 (Orchestration, maps to AGF Security Governance). Distinct from Wrapper (one deployment mode), Hypervisor (AGT-specific runtime isolation kernel), and Test Harness (general software engineering). | Industry has converged on this definition across Anthropic ("Agent = Model + Harness"), Parallel Web Systems, Philschmid, Atlan, and the Kim & Hwang SSRN paper. AGF's own Agent Harness Spec v0.3 uses the same framing. Earlier internal notes that drew a hard line between design-time scaffolding and runtime enforcement were inconsistent with industry usage and the spec; the Source → Build → Runtime pattern lives *within* the harness, not as a distinction between harness and something else. | Reserve "harness" for design-time scaffolding only (rejected — conflicts with industry usage and AGF's own spec); avoid naming "harness" at all (rejected — it's becoming a load-bearing term in the field; AGF needs a canonical definition); treat "harness" and "wrapper" as synonyms (rejected — wrapper is one deployment pattern; harness is the governance envelope regardless of deployment pattern). |
