# ── Private working directory ──
.private/

# ── Claude Code config (session-specific) ──
.claude/

# ── Research inbox (external PDFs + upstream repo clones — too large for git) ──
docs/inbox/

# ── OS ──
.DS_Store

# ── Session data ──
data/
*.db
*.db-shm
*.db-wal

# ── Node / Next.js (agf-docs) ──
agf-docs/node_modules/
agf-docs/.next/
agf-docs/.source/
.env*.local

# ── Build artifacts ──
*.tsbuildinfo
